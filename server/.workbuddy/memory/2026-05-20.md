# 2026-05-20

## NarrativeEvent 交互优化
- 修改 `src/components/narrative/NarrativeEvent.tsx`
- 将剧情事件模态框从"仅按钮交互"改为"点击任意处继续"
- 具体改动：
  1. 新增 `handleClickAnywhere`：打字未完成时跳过打字机，打字完成且无 choices 时关闭节点
  2. 外层容器绑定 `onClick={handleClickAnywhere}`，添加 `cursor-pointer`
  3. 移除内容卡片的 `stopPropagation`，让点击事件自由穿透
  4. 无 choices 时的底部提示改为「点击任意处继续」并带 pulse 动画
- 处理了 `hasChoices` 在 `useCallback` 依赖中前置声明的 TypeScript 块级作用域问题（用 `maybeNode` 提前计算）

## 项目运行
- Vite dev server 已启动在 http://localhost:5173
